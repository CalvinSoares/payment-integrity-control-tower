spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5438/payment_integrity}
spring.datasource.username=${POSTGRES_USER:integrity}
spring.datasource.password=${POSTGRES_PASSWORD:integrity_dev}
spring.datasource.hikari.maximum-pool-size=${CONTROL_TOWER_DB_POOL_MAX:10}
server.port=${CONTROL_TOWER_JAVA_PORT:8080}
